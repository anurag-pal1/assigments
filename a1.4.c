#include<stdio.h>
     #define PI 3.14
      int main()
     {
          float R,A;
          printf("The radius of circle is:");
          scanf("%f",&R);
          A=PI*R*R;
          printf("\nArea of circle is %f having the radius %f",A,R);
          return 0;
     }

